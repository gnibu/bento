---
description: Wire bento into the current environment — vendored Claude plugins, principles (user CLAUDE.md), Codex native skills and the AGENTS.md principles block, and the update-check hook (SessionStart). Idempotent; run once per project after installing bento. Args — none = interactive; `--yes` = batch defaults; `--purge` = unwire.
---

Read and follow `${CLAUDE_PLUGIN_ROOT}/references/bento-setup.md`.
Those shared instructions are used by both Claude and Codex.
