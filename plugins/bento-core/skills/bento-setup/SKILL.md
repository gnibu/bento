---
name: bento-setup
description: Wire bento into the current environment — vendored Claude plugins, principles (user CLAUDE.md), Codex native skills and the AGENTS.md principles block, and the learn hooks (Stop + SessionStart). Idempotent; run once per project after installing bento. Args — none = interactive; `--yes` = batch defaults; `--purge` = unwire.
---

Read and follow [the shared instructions](../../references/bento-setup.md).
Resolve this link from this skill’s real location inside the Bento plugin, following
symlinks if installed under `.codex/skills/`.
