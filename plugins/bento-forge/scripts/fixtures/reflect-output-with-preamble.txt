Not already documented. Emitting the two findings.

{"key":"check-hook-mypy-vendored-venv-files","sink":"code","summary":"check.sh's route() has no exclusion for vendored paths, so any Bash command touching a .py file under backend/.venv (grep, cat, sed) trips a spurious mypy hook failure that can never be fixed.","evidence":"HOOK: mypy failed: ... backend/.venv/lib/python3.12/site-packages ... is in the MYPYPATH. Please remove it. — recurred 4x in this session while the agent inspected vendored lightrag source and each time concluded 'just a vendored file read... disregard'.","proposal":"In .claude/hooks/check.sh route(), add a case before backend/*.py) that returns skip for paths containing /.venv/ or /site-packages/; add a matching --selftest assertion."}
{"key":"zsh-glob-arg-expansion","sink":"agents-md:AGENTS.md","summary":"Unquoted glob metacharacters in a Bash argument (e.g. grep --include=*.py) get glob-expanded by zsh itself before the command runs, erroring 'no matches found' instead of reaching the target program.","evidence":"ERR: Exit code 1 / (eval):1: no matches found: --include=*.py — hit twice in this session with the identical error.","proposal":"Add a bullet to root AGENTS.md's Bash Tool > Guidelines, next to the existing zsh word-splitting line: quote glob-like flag values (--include='*.py'), zsh expands them itself pre-command."}
